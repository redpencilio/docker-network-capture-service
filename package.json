{
  "name": "docker-network-capture-service",
  "version": "1.0.0",
  "description": "a service that captures network traffic of a specific container and saves it to pcap files",
  "main": "index.js",
  "dependencies": {
    "dockerode": "^2.5.5",
    "fs-extra": "^7.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "redpencil.io",
  "license": "MIT"
}
